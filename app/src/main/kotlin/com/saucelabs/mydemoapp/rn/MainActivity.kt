package com.saucelabs.mydemoapp.rn

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

/**
 * Stub MainActivity for the test harness.
 * The real app under test (Sauce Labs MyDemoApp RN) provides its own Activity.
 * This stub ensures the project compiles as a valid Android application module.
 */
class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}
